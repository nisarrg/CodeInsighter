stages:
  - build
  - test
  - smell
  - deploy
 
build:
  stage: build
  image: maven:latest
  script:
    - echo "Building the pipeline"
    - mvn clean package
  artifacts:
    paths:
      - target
